# Configuration Docker avec CORS
# Utilisez ce fichier pour les déploiements Docker

# Base de données
DB_HOST=db
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=facturation
DB_PORT=5432

# JWT
JWT_SECRET=your-production-secret-key-here

# Serveur
PORT=8080

# Configuration CORS pour Docker
GO_ENV=production
ALLOWED_ORIGINS=http://localhost:3000,http://frontend:3000,http://nginx:80
PROD_FRONTEND_URL=http://localhost:80
