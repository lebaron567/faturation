# Configuration CORS et JWT pour l'API Facturation

# URL de l'API backend
REACT_APP_API_URL=http://localhost:8080

# Configuration pour le développement
# En production, changez ces valeurs
REACT_APP_ENV=development

# Debug mode (affiche les logs détaillés)
REACT_APP_DEBUG=true

# Token storage key (optionnel, par défaut: auth_token)
REACT_APP_TOKEN_KEY=auth_token

# Configuration CORS (pour information)
# Ces origines doivent être configurées côté backend :
# - http://localhost:3000 (dev React)
# - http://localhost:3001 (port alternatif)
# - https://votre-domaine.com (production)

# Note: Le JWT_SECRET doit être configuré côté BACKEND uniquement
# Exemple backend: JWT_SECRET=Fact2025-SuperSecret-Key-EmericBarotin-9x8z7y6w5v4u3t2s1r0q
